# Yomitoki アプリ概要

Yomitoki は、AI エージェントが生成したプラン、実行結果、調査メモ、文書化の途中成果をまとめて回収し、あとから読める形で残すための個人用ダッシュボードです。  
単なるノートアプリではなく、**プラン収集・整理・閲覧・要約・文書生成・バックアップ** までを一つの流れとして扱うのが特徴です。

## 1. 何を解決するアプリか

AI と作業していると、承認されたプランや会話の文脈は流れ去りがちです。Yomitoki はその断片を `scraps` として集約し、後から見返せる「作業の記憶」にします。

- 何を考えていたかを残す
- 何を決めたかを残す
- どのプランから何が生まれたかを残す
- 必要なら文書として再構成する

## 2. 主な機能

### 2.1 プランの保存

`/plan` で生成された Markdown を保存し、`plan` 系のスクラップとして蓄積します。  
`plans:save` コマンドは以下をまとめて扱います。

- title / slug の登録
- project / directory のメタデータ保存
- created_from の記録
- slug の重複回避
- embedding / summary ジョブの起動

### 2.2 実行結果の紐付け

`plans:result` コマンドで、実行結果を既存プランの子スクラップとして保存できます。

- 元プランとの親子関係を保持
- 実行結果も検索・要約対象にする
- 実行した流れを時系列で追える

### 2.3 ダッシュボード

ダッシュボードはアプリの中心です。

- インボックス表示
- タグ絞り込み
- active / archived 切り替え
- 個別スクラップ詳細
- 子スクラップの表示
- 最近の AI 実行状況
- 最近の文書
- ソース種別の集計
- 複数選択による一括操作

一括操作では、アーカイブ、文書生成、まとめてバックアップができます。

### 2.4 Articles ワークスペース

`articles` 画面は、トップレベルのスクラップを記事のように読むためのビューです。

- slug で詳細ページを開ける
- archived 記事も閲覧可能
- バックアップ履歴を同じ画面から見られる
- Markdown 本文を読みやすく表示する

### 2.5 バックアップ

バックアップ機能は、記録を「持ち出せる」状態にします。

- 単体スクラップの ZIP 化
- 複数スクラップの一括バックアップ
- SSE で進捗を表示
- 画像資産も同梱
- バックアップ履歴を追跡

### 2.6 AI による文書生成

複数のプランやスクラップをもとに、AI が文書を再構成します。

- 仕様書
- 概要ドキュメント
- レポート

AI 実行は `AiRun` で記録され、生成結果は `Document` として保存されます。  
元のスクラップとの関連も保持されるため、どの材料から何ができたかを追えます。

### 2.7 要約とタイトル整形

`SummarizeScrapAgent` により、スクラップのタイトルや要約を日本語で整えます。  
断片的なメモでも、一覧で読みやすい形に近づけられます。

### 2.8 Markdown 表示

本文は Markdown として扱い、表示時には次をサポートします。

- 見出し
- 箇条書き
- 引用
- 表
- コードブロック
- インラインコード
- リンク

コードブロックは `shiki` でシンタックスハイライトされます。

## 3. このアプリの強み

### 3.1 AI 作業の「途中経過」が残る

AI の出力を最終成果物だけでなく、プラン・実行結果・文書化まで含めて残せます。  
あとから「なぜその実装になったのか」を振り返りやすいです。

### 3.2 一覧と詳細の両方が強い

一覧では流れを追い、詳細では Markdown をそのまま読めます。  
読む場所と整理する場所が同居しているので、情報の往復が少ないです。

### 3.3 記録が再利用しやすい

プランを文書化したり、結果を親プランへ戻したりできるので、単なるアーカイブではなく再編集の土台になります。

### 3.4 画像込みで保存できる

テキストだけでなく画像もバックアップできるため、ビジュアルを含む記録に向いています。

### 3.5 日本語中心の運用に寄せている

UI と要約が日本語ベースなので、AI 作業の記録を自然に読み返せます。

## 4. 改善点

### 4.1 発見性の強化

現状でもタグや一覧はありますが、さらに以下があると探しやすくなります。

- 全文検索の強化
- 関連スクラップの自動推薦
- タイムライン表示
- プラン同士のリンク可視化

### 4.2 生成の信頼性向上

AI 生成は便利ですが、出力の品質が揺れる可能性があります。

- 生成前後の差分確認
- 失敗時の再実行導線
- 生成テンプレートの固定化
- 出力検証の厳格化

### 4.3 ワークフローの明示化

今は「保存された記録」を中心にしていますが、次の段階では状態遷移を見せるとさらに扱いやすくなります。

- plan
- result
- document
- archived
- published

### 4.4 複数プロジェクト対応

現状は個人の作業記録として強い一方、複数リポジトリをまたぐと整理軸が増えます。

- プロジェクト別ビュー
- リポジトリ別集約
- 送信元認証
- チーム共有の導線

### 4.5 編集体験の拡張

読むだけでなく、アプリ内で少し整えられると使いやすくなります。

- 下書き編集
- 要約の再生成
- タイトルの手修正
- タグ付けの補助

## 5. さらなる発展の可能性

### 5.1 「作業ログ」から「知識ベース」へ

今は記録の収集が主ですが、蓄積が進むと知識ベースとして使えます。

- 類似プランの検索
- 過去の意思決定の参照
- よく使う設計パターンの抽出

### 5.2 AI による構造化支援

記録をただ要約するだけでなく、構造化もできます。

- 重要度の推定
- リスクの抽出
- 未解決事項の一覧化
- 実装タスクへの分解

### 5.3 チーム運用

個人用からチーム用に広げるなら、以下が有効です。

- 権限管理
- 共有スペース
- 承認フロー
- プロジェクト横断検索

### 5.4 外部連携

他ツールとつなぐと、記録の価値が上がります。

- GitHub Issues / PR
- Notion / Obsidian
- Slack / Discord 通知
- CI 結果の自動取り込み

### 5.5 使うほど賢くなる体験

将来的には、過去の記録を踏まえて提案するアシスタントにもできます。

- 「このテーマの前回の結論」
- 「似たプランの実行結果」
- 「失敗しやすい構成」
- 「次に読むべき関連記録」

## 6. まとめ

Yomitoki は、AI 作業の断片を集めて、**読む・比べる・残す・再利用する** ためのアプリです。  
強みは、プランと結果と文書を一つの流れで扱えること。  
今後は、検索性・生成信頼性・チーム共有・外部連携を伸ばすことで、単なる記録アプリから知識基盤へ発展できます。
